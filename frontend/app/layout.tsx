import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ChronoCraft — Autonomous Planetary Weather Strategy MMO & Siege Engine',
  description: '4X Planetary strategy game where on-chain resource yield and siege defense are driven by live satellite weather telemetry.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
